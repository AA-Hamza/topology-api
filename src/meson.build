sources = ['device.cpp', 'topology.cpp', 'topologyAPI.cpp', 'topologyList.cpp']
topologylib = library('topologylib',
                     sources,
                     include_directories: inc,
                     dependencies: json,
                     install: true)

